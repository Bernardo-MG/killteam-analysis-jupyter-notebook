

def user_value(current, toSet):
    """
    Sets the user value to the one received.
    """
    if current == "user":
        result = str(toSet)
    else:
        result = current

    return result
